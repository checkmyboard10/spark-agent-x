import StartNode from './StartNode';
import EndNode from './EndNode';
import MessageNode from './MessageNode';

export const nodeTypes = {
  start: StartNode,
  end: EndNode,
  message: MessageNode,
};

export { StartNode, EndNode, MessageNode };
